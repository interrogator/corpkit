# project definitions
project: doc
audience: geeks
product: all
platform: all
version: all
destination: ../doc_geeks
topnav_title: corpkit
homepage_title: corpkit&#58; a Python module for corpus linguistics
site_title: corpkit
disqus_shortname: mcdonaldd
google_analytics: 

# variables

sidebar_tagline: geeks
sidebar_version: graphical interface
theme_file: theme-green.css
project_file_name: doc

port: 4003
exclude:
  - doc_non-coders*
  - configs/
  - _site
  - _drafts
  - di_multi*

# same for all
host: 127.0.0.1
feedback_email: mcdonaldd@unimelb.edu.au
sidebar_accordion: true
markdown: redcarpet
print: false

# only use suffix if you need to force index.html to display
# suffix: index.html

redcarpet:
  extensions: ["no_intra_emphasis", "fenced_code_blocks", "tables", "with_toc_data"]

highlighter: pygments

collections:
  tooltips:
    output: true

defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
      comments: true
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      comments: true


